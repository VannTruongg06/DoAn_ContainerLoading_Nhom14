#ifndef GENETIC_ALGORITHM_H
#define GENETIC_ALGORITHM_H

#include "models.h"
#include <vector>

// Hàm xếp hàng theo giải thuật di truyền
void geneticAlgorithmPacking(Container& container, std::vector<Item>& items);

#endif // GENETIC_ALGORITHM_H
